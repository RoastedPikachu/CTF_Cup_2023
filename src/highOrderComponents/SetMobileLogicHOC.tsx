import React from 'react';

import { useIsMobileDevice } from "@/customHooks";

const SetMobileLogicHoc = () => {
    const isMobile = useIsMobileDevice();

    return (
        <div>
            
        </div>
    );
};

export default SetMobileLogicHoc;